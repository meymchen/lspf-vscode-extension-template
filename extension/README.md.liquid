# {{ display_name }}

VS Code language support for {{ display_name }}, backed by a Rust language
server built with [lspf](https://github.com/meymchen/lspf).

Open a `{{ file_extension }}` file to activate the extension. Replace the
example diagnostic, hover, and completion handlers with your language logic.
